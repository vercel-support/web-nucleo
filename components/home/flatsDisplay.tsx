import Flat from "../../backend/salesforce/flat";

type Props = {
    flats: Flat[];
}
  
const FlatsDisplay = ({ flats }: Props) => {
  return <div>FlatsDisplay</div>;
};
export default FlatsDisplay;
